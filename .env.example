# Neon Postgres (with pgvector extension enabled)
DATABASE_URL=postgresql://user:password@host.neon.tech/politscope?sslmode=require

# Embedding provider: OpenAI text-embedding-3-small (1536-dim) — locked in #6
OPENAI_API_KEY=

# Cron secret to gate /api/cron routes from public access on Vercel
CRON_SECRET=replace-with-strong-random-string
